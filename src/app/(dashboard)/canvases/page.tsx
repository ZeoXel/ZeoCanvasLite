import DashboardCanvasList from "@/components/dashboard/DashboardCanvasList";

export const metadata = {
    title: "ZeoCanvas",
};

export default function Page() {
    return <DashboardCanvasList />;
}
